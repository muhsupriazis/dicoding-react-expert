import styled from 'styled-components';

const Icon = styled.div`
  color: #00595c;
  font-size: .9rem;
  display: flex;
  align-items: center;
  gap: .5rem;
  padding: 1rem 0;
`;

export default Icon;
